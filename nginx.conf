server {
    listen 80;

    # Frontend (React)
    location / {
        proxy_pass http://frontend:80;
    }

    # Node.js backend
    location /api/ {
        proxy_pass http://backend:5000/;
    }

    # Flask AI backend
    location /ai/ {
        proxy_pass http://flask-ai:5001/;
    }
}
